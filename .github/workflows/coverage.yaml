---
name: Go coverage

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  coverage_checks:
    runs-on: ubuntu-latest
    name: coverage
    timeout-minutes: 20
    steps:
      - name: Checkout current PR
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      # The former Code Climate test-reporter integration is gone; the
      # download endpoint now serves an HTML stub. Coverage is generated and
      # published as a workflow artifact with a summary in the job output.
      - name: Generate coverage
        run: go test -covermode=atomic -coverprofile=coverage.out ./...

      - name: Coverage summary
        run: go tool cover -func=coverage.out | tail -5

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out

      # Feeds the README codecov badge. Tolerates a missing CODECOV_TOKEN
      # secret so the job stays green if the integration is ever detached.
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: coverage.out
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
